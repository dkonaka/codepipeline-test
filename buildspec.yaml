
version: 0.2
phases:
  install:
    commands:
      - npm install time
      - aws cloudformation package --template-file samTemplate.yaml --s3-bucket codedeploy-linux
                                   --output-template-file outputSamTemplate.yaml
artifacts:
  type: zip
  name: InputArtifactsss
  files:
    - samTemplate.yaml
    - outputSamTemplate.yaml
